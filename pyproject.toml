[project]
name = "squishy-mcsquishface"
version = "0.2.1"
description = "Drop a video, pick a compression level, get a smaller MP4."
license = "MIT"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
